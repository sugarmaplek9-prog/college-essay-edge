# Tasks — 072 Real Human Corpus Expansion

1. Freeze `074` completely as a closed stop-state lane and keep blind untouched.
2. Normalize `072` queue, intake, registry, and split artifacts to the V2 metadata contract.
3. Preserve the frozen first-pass `15/8/2` bootstrap as a truth artifact; do not reshuffle cases.
4. Record the post-`074` convergence interpretation: pass 1 improved a real class, pass 2 did not move the final miss, and no successor lane is justified yet.
5. Continue broader real-human evidence gathering through the sourcing queue, prioritizing underrepresented medium and weak rough-input forms and more varied source domains.
6. Use broader evidence to determine whether the unresolved behavior is a one-off outlier or the start of a genuinely new class.
7. Open no new lane unless a clean class-definition memo can be written with inclusion criteria, exclusion criteria, and proof preserves are outside the class.
8. Keep blind closed until broader evidence and a visible-first packet justify any later gate.
9. Treat the blind framework artifacts in `075` as complete but non-executable for the current frozen split.
10. Return to `072` as the only active program lane and prepare a future frozen split containing new real-human cases, fresh blind-assigned cases, and cases never exposed to repair work.
11. Do not revisit blind review until that future frozen split exists and a fresh executable blind pack can be regenerated leakage-safe.
12. Use `future-frozen-split-objective-v1.md` as the governing next objective for all subsequent sourcing work.
13. Start the next sourcing batch with one purpose only: collect enough new real-human cases to freeze a later split with fresh blind-assigned, never-exposed cases.
14. Convert the completed blind human review into `proof-memo-v1.md` summarizing the internal advantage evidence without authorizing a public proof claim.
15. Create `targeted-repair-plan-v1.md` for the modest-advantage pattern, covering templating, phrasing, decisiveness, visibility of wins, and stronger student-specific grounding.
16. Require all subsequent repair work to run through repeatable evaluation with fixed inputs, written rubric, pass/fail threshold, artifacted results, and GitHub PR evidence.
